import { Card } from "@shopify/polaris";
import React from "react";

const ProductSettings = () => {
  return (
    <Card title="Products Settings">
      <p>Products Settings</p>
    </Card>
  );
};

export default ProductSettings;
